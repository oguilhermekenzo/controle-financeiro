{
  "name": "meu-financeiro-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && esbuild index.tsx --bundle --outfile=dist/bundle.js --minify --sourcemap && cp -r public/. dist/"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.43.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "recharts": "^2.12.7"
  },
  "devDependencies": {
    "@types/react": "^18.2.48",
    "@types/react-dom": "^18.2.18",
    "esbuild": "^0.19.11",
    "typescript": "^5.3.3"
  }
}