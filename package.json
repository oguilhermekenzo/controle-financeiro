{
  "name": "meu-financeiro-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/components && cp index.html dist/ && (cp -r components/images dist/components/images || true) && esbuild index.tsx --bundle --outfile=dist/bundle.js --minify --sourcemap"
  },
  "dependencies": {
    "@google/genai": "^0.14.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-markdown": "^9.0.1",
    "recharts": "^2.12.7"
  },
  "devDependencies": {
    "@types/react": "^18.2.48",
    "@types/react-dom": "^18.2.18",
    "esbuild": "^0.19.11",
    "typescript": "^5.3.3"
  }
}